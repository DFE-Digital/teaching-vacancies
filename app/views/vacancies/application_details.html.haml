%h1.heading-large
  Publish a vacancy for
  = @vacancy.school.name
  %span.heading-secondary
    Step 3 of 3
- if @vacancy.errors.any?
  .error-summary{role: 'alert', tabindex: '-1'}
    %h2.heading-medium.error-summary-heading
      = pluralize(@vacancy.errors.count, 'error')
      prevented this vacancy from being saved:
    %ul
      - @vacancy.errors.full_messages.each do |message|
        %li= message
= simple_form_for @vacancy do |f|
  = hidden_field_tag :from, :application_details
  = hidden_field_tag :next, :review
  = render partial: 'vacancy_details', locals: { f: f }

  = f.button :submit, 'Save and continue'
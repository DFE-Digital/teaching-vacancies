= f.govuk_collection_select :radius,
  radius_filter_options,
  :last,
  :first,
  label: { text: t("jobs.filters.within_radius"), size: "s" },
  class: %w[govuk-!-width-full],
  form_group: { classes: "#{filters ? 'govuk-inset-text' : ''} location-radius-select location-radius-select-block" }

<%= t(".greeting", jobseeker_name: @jobseeker_name) %>

<%= t(".invitation", publisher_name: @publisher_name, organisation_name: @organisation_name, count: @vacancies.count)%>

<%- @vacancies.each do |vacancy| %>
  <%= invitation_to_apply_vacancy_link(vacancy) %>
<% end %>

# <%= t(".get_support.heading")%>

<%= t(".get_support.body", link: (notify_mail_to t("help.email")) )%>

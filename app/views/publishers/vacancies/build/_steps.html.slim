= render StepsComponent.new title: t("publishers.vacancies.review.steps"), classes: "govuk-!-margin-top-6" do |component|
  - steps_to_display(steps_config, steps_adjust).each do |step, step_number|
    - component.step(label: t("publishers.vacancies.build.#{step}.step_title"), current: (step == step_current), completed: vacancy_step_completed?(vacancy, step_number))

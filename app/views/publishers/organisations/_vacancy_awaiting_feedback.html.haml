-# %tr.govuk-table__row.vacancy[vacancy]
-#   = form_for vacancy, url: organisation_job_statistics_path(vacancy.id), html: { id: vacancy.id, method: 'patch', class: 'vacancy-feedback__form', data: { option_not_selected_message: t('messages.jobs.feedback.inline_error') } } do |f|
-#     %td.govuk-table__cell
-#       = link_to vacancy.job_title, vacancy.preview_path, class: 'govuk-link view-vacancy-link', id: 'job-title'
-#     - if organisation.is_a?(SchoolGroup)
-#       %td.govuk-table__cell#vacancy_location= vacancy.readable_job_location
-#     %td.govuk-table__cell= vacancy.expires_on
-#     %td.govuk-table__cell{ class: 'govuk-!-width-one-quarter' }= select_tag 'vacancy[hired_status]', options_for_select(hired_status_options, nil), form: vacancy.id, include_blank: '--', class: 'govuk-select form-control form-control-block govuk-!-width-full'
-#     %td.govuk-table__cell{ class: 'govuk-!-width-one-quarter' }= select_tag 'vacancy[listed_elsewhere]', options_for_select(listed_elsewhere_options, nil), form: vacancy.id, include_blank: '--', class: 'govuk-select form-control form-control-block govuk-!-width-full'
-#     %td.govuk-table__cell= submit_tag t('buttons.submit'), form: vacancy.id, class: 'govuk-button govuk-!-margin-bottom-0'

.card
  = form_for vacancy, url: organisation_job_statistics_path(vacancy.id), html: { id: vacancy.id, method: 'patch', class: 'vacancy-feedback__form', data: { option_not_selected_message: t('messages.jobs.feedback.inline_error') } } do |f|
    .card__contents
      %dl.card__title
        %dd.govuk-body-l= govuk_link_to(vacancy.job_title, vacancy.preview_path, class: 'view-vacancy-link', id: 'job-title')
      %dl.card__details
        - if organisation.is_a?(SchoolGroup)
          %dd= vacancy.readable_job_location
        %dd.govuk-body= t('jobs.closing_date_html', date: vacancy.expires_on)

    %dl.card__actions{ class: 'vacancy__feedback' }
      %dd
        %ul.govuk-list
          %li{ class: 'govuk-!-width-full-width' }= select_tag 'vacancy[hired_status]', options_for_select(hired_status_options, nil), form: vacancy.id, include_blank: '--', class: 'govuk-select form-control form-control-block govuk-!-width-full'
          %li{ class: 'govuk-!-width-full-width' }= select_tag 'vacancy[listed_elsewhere]', options_for_select(listed_elsewhere_options, nil), form: vacancy.id, include_blank: '--', class: 'govuk-select form-control form-control-block govuk-!-width-full'

    %dl.card__actions
      %dd
        %ul.govuk-list
          %li= submit_tag t('buttons.submit'), form: vacancy.id, class: 'govuk-button govuk-!-margin-bottom-0'

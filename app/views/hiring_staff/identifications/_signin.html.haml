- home = false if local_assigns[:home].nil?
= form_for :identifications, url: identifications_path do |f|
  = f.govuk_submit t('sign_in.link'), secondary: true, classes: "#{home ? 'identifications-button' : ''} govuk-!-margin-bottom-0"

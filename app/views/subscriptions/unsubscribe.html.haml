- content_for :page_title_prefix, t('.title')

.govuk-grid-row
  .govuk-grid-column-two-thirds
    .govuk-panel.tvs-panel.govuk-panel--confirmation
      = render partial: 'shared/checkmark'

      %h1.govuk-panel__title= t('.header')
      .govuk-panel__body
        %p= t('.confirmation')

  .govuk-grid-column-two-thirds
    %p.govuk-body=t('.resubscribe_html', link: link_to(t('.resubscribe_link_text'), new_subscription_path(search_criteria: @subscription.search_criteria_to_h), class: 'govuk-link', id: 'job-alert-link-from-cancel-unsubscribe-gtm'))

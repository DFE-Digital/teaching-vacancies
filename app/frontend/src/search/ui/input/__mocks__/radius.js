export const enableRadiusSelect = jest.fn();

export const disableRadiusSelect = jest.fn();

export const getRadius = jest.fn();

const radiusSelect = {
  enableRadiusSelect,
  disableRadiusSelect,
};

export default radiusSelect;

require "rails_helper"

RSpec.describe Publishers::JobListing::PayPackageForm, type: :model do
  # it { is_expected.to validate_presence_of(:salary) }
  # it { is_expected.to validate_length_of(:salary).is_at_most(256) }
  # it { is_expected.to validate_presence_of(:actual_salary) }
  # it { is_expected.to validate_length_of(:actual_salary).is_at_most(256) }
  # it { is_expected.to validate_presence_of(:pay_scale) }
  # it { is_expected.to validate_length_of(:pay_scale).is_at_most(256) }
end
